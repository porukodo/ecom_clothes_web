# OS
.DS_Store

# XAMPP / PHP
/vendor/
# NOTE: .env is intentionally committed for this course project so that
# classmates / lecturers can clone-and-run without generating their own keys.
# In a real production system, .env MUST be in .gitignore.
# .env
.env.generated
*.pem

# Logs
*.log

# Cache
/tmp/

# Benchmark outputs (regenerate locally)
scripts/benchmark_results_*.json
scripts/benchmark_comparison.json
scripts/benchmark_report.html