<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /ecom_clothes_web/public/

    # Chỉ xử lý các request mà file hoặc thư mục tương ứng không tồn tại
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>